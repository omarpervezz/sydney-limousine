/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.pexels.com", "sydneylimousineservice.com"], // Add the external domain here
  },
};

export default nextConfig;
